#include<iostream>

using namespace std;

#define Maxnum 20000000
int main() {
    vector<bool> prime(Maxnum, true);
    for (int i = 1; i < Maxnum; i ++) {
        prime[i] = true;
    }
    int pos = 2, temp;
    while(pos < Maxnum) {
        temp = pos
        if (prime[temp] == true) {
            for (int i = )
        }
        pos ++;
    }
}
